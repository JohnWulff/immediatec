/********************************************************************
 *
 *	simple iC program with arrays
 *
 *******************************************************************/

%{
%#include	<stdio.h>
%}

    if (IX0.0) {
	printf("%d: Immediate Nr. %d\n", 0, 2);	/* { */
    }
    if (IX0.1) {
	printf("%d: Immediate Nr. %d\n", 1, 2);	/* { */
    }
    if (IX0.2) {
	printf("%d: Immediate Nr. %d\n", 2, 2);	/* { */
    }
QX0.7 = IX0.6 & IX0.7;
